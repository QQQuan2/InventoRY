# 后端类型：sqlite（默认） 或 mysql
DB_BACKEND=sqlite

# SQLite 配置（DB_BACKEND=sqlite 时生效）
SQLITE_PATH=waimai.db

# MySQL 配置（DB_BACKEND=mysql 时生效）
MYSQL_HOST=127.0.0.1
MYSQL_PORT=3306
MYSQL_USER=root
MYSQL_PASSWORD=请改成你自己的密码
MYSQL_DB=waimai
MYSQL_CHARSET=utf8mb4